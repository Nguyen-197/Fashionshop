
import { forwardRef, useEffect, useRef, useState } from 'react';
import { IFieldProps } from '../interface/field-prop';
import _ from 'lodash';
import { FieldStyleClass } from './field-style-class';

type IPrimaryKeyControlProps = IFieldProps & {
}
// https://formik.org/docs/examples/dependent-fields
const PrimaryKeyControl = forwardRef((props: IPrimaryKeyControlProps, ref) => {

    return (
        <>
            
        </>
    )
});


export default PrimaryKeyControl;
